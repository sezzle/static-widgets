[test]
preload = ["./tests/setup.js"]

[install]
# Match npm's flat node_modules layout
linker = "hoisted"

# Refuse to install package versions published in the last 3 days (supply-chain safety)
minimumReleaseAge = 259200
minimumReleaseAgeExcludes = [
  "@sezzle/sezzle-static-widget"
]
